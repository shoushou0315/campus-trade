<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.trade.mapper.CategoryMapper">

    <resultMap id="categoryResultMap" type="com.campus.trade.entity.CategoryDO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="parentId" column="parent_id"/>
        <result property="sort" column="sort"/>
        <result property="icon" column="icon"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModified" column="gmt_modified"/>
    </resultMap>

    <select id="selectAll" resultMap="categoryResultMap">
        SELECT id, name, parent_id, sort, icon, gmt_create, gmt_modified
        FROM category
        ORDER BY sort ASC
    </select>

    <select id="selectById" resultMap="categoryResultMap">
        SELECT id, name, parent_id, sort, icon, gmt_create, gmt_modified
        FROM category
        WHERE id = #{id}
    </select>

</mapper>
